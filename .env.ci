# CI Environment Variables
# Use this file in CI/CD pipelines to override development defaults

# Qdrant container image
QDRANT_IMAGE="qdrant/qdrant:1.8.2"

# CI-appropriate restart policy - limits retries to prevent infinite crash loops
# This helps surface errors quickly in CI rather than masking them with restarts
QDRANT_RESTART_POLICY="on-failure:3"
