# Docker Compose Environment Variables

# Qdrant container image
# Recommendation: pin to a specific version or digest for reproducibility (especially in Prod/CI).
# Example pinned tag (update as needed):
# QDRANT_IMAGE=qdrant/qdrant:v1.9.0
# Example digest pin (update as needed):
# QDRANT_IMAGE=qdrant/qdrant@sha256:1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef
QDRANT_IMAGE=qdrant/qdrant:latest

# Restart policy for Qdrant service
# Local/Dev: unless-stopped (default) - service restarts unless manually stopped
# CI/Testing: on-failure:3 - limits retries to 3, surfaces crash loops (see .env.ci)
# Production: unless-stopped or on-failure:5 depending on requirements
QDRANT_RESTART_POLICY=unless-stopped
